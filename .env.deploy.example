# Liquid Web Spark Launch SFTP credentials (from Site Dashboard → SFTP/SSH)
# Copy to .env.deploy and fill in — never commit .env.deploy

SFTP_HOST=your-site.sftp.host
SFTP_PORT=22
SFTP_USER=your-sftp-username
SFTP_PASSWORD=your-sftp-password
SFTP_REMOTE_DIR=/chroot/home/a13300ff/flylre.com/html
